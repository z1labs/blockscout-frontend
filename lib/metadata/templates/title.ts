import type { Route } from 'nextjs-routes';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function make(pathname: Route['pathname'], isEnriched: boolean) {
  return 'Cypher Testnet Epoch 1 | Zero1 Labs';
}
